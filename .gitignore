# MacOS specific
.DS_Store

# All the files are generated during run time reside in `.tmp` folder
.tmp

# OTA files
*.zip

# Keys and secrets
.keys
# generate_keys writes these into the working directory. avb_pkmd.bin is left
# out on purpose: it is the public half and a fork may want to commit it
avb.key
ota.crt
ota.key

# A local build leaves the signature and the update info beside the sources
*.csig
/*.json
!/.prettierrc.json

# Python virtual environment
venv

# OMO
.omo
