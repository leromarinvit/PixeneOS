name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  smoke:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Check shell syntax
        run: |
          for file in src/*.sh src/ci/*.sh; do
            bash -n "$file"
          done

      - name: Shellcheck
        # `-x` makes a sourced file's variables visible to its consumer but reports
        # no findings inside it, so the library is listed too
        run: shellcheck -x -S warning src/*.sh src/ci/*.sh

      - name: Check env.toml parsing
        shell: bash
        run: |
          source src/util_functions.sh && check_toml_env

      - name: Check the release script exports its outputs
        run: src/ci/check_source_contract.sh

      - name: Check download URLs
        run: src/ci/smoke_test.sh
